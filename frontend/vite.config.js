// vite.config.js
// Basic Vite setup for a React project - nothing fancy added.

import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
    plugins: [react()]
});
