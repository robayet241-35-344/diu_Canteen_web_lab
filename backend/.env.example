# Copy this file to ".env" and fill in your own MySQL details.
# .env is in .gitignore, so your real password never gets pushed to GitHub.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=canteen_db
PORT=5000
